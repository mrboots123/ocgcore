import {createAction} from "@reduxjs/toolkit";

export const msg_draw = createAction('MSG_DRAW');

export const msg_new_turn = createAction('MSG_NEW_TURN');

export const msg_new_phase = createAction('MSG_NEW_PHASE');

export const msg_select_idlecmd = createAction('MSG_SELECT_IDLECMD');
